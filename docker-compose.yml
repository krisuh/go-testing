version: '3'
services:
  edge_service:
    build: .
    image: tyhjataulu/go-blinker:latest
    ports:
      - 8080:8080
    privileged: true
    deploy:
      mode: global
      placement:
        constraints:
          - node.labels.location == edge
          - node.labels.arch == arm