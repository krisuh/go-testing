version: '3'
services:
  edge_service:
    build: .
    image: tyhjataulu/go-blinker:latest
    ports:
      - "8080:8080"
    deploy:
      replicas: 2
      placement:
        constraints:
          - node.labels.location == edge